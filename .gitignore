# secrets — never commit
.env
*.env
!*.env.example
!.env.example

# local runtime
venv/
__pycache__/
*.log
*.db
*.sqlite3

# the Odysseus app is cloned separately (see README) — not vendored here
odysseus/
